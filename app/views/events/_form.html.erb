<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Title %><br>
    <%= f.text_field :Title %>
  </div>
  <div class="field">
    <%= f.label :Date %><br>
    <%= f.text_field :Date %>
  </div>
  <div class="field">
    <%= f.label :What_Happened %><br>
    <%= f.text_area :What_Happened %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
